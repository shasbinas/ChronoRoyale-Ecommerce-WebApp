<main class="main">
  <section class="mt-50 mb-50">
    <div class="container">
      <div class="row">
        <div class="col-12">
          <div class="table-responsive">
            <table class="table shopping-summery text-center clean">
              <thead>
                <tr class="main-heading">
                  <th scope="col">Image</th>
                  <th scope="col">Name</th>
                  <th scope="col">Price</th>
                  <th scope="col">Quantity</th>
                  <th scope="col">Subtotal</th>
                  <th scope="col">Remove</th>
                </tr>
              </thead>
              <tbody>
                {{#each cart}}
                <tr>
                  <td class="image product-thumbnail">
                    <img src="{{this.image}}" alt="{{this.name}}">
                  </td>
                  <td class="product-des product-name">
                    <h5 class="product-name"><a href="#">{{this.name}}</a></h5>
                    <p class="font-xs">{{this.shortDescription}}</p>
                  </td>
                  <td class="price" data-title="Price">
                    <span>₹{{this.price}}</span>
                  </td>
                  <td class="text-center" data-title="Quantity">
                    <div class="detail-qty border radius m-auto">
                      <a href="#" class="qty-down"><i class="fi-rs-angle-small-down"></i></a>
                      <span class="qty-val">{{this.quantity}}</span>
                      <a href="#" class="qty-up"><i class="fi-rs-angle-small-up"></i></a>
                    </div>
                  </td>
                  <td class="text-right" data-title="Subtotal">
                    <span>₹{{this.total}}</span>
                  </td>
                  <td class="action" data-title="Remove">
                    <a href="/cart/remove/{{this.productId}}" class="text-muted">
                      <i class="fi-rs-trash"></i>
                    </a>
                  </td>


                </tr>
                {{/each}}
                 <td colspan="6" class="text-end">
  <a href="/cart/clear" class="text-muted"> 
    <i class="fi-rs-cross-small"></i> Clear Cart
  </a>
</td>

              </tbody>
            </table>
          </div>

         
        </div>

        <div class="row mb-50 mt-50">
          <div class="col-lg-6 col-md-12">
            <div class="border p-md-4 p-30 border-radius cart-totals">
              <div class="heading_s1 mb-3">
                <h4>Cart Totals</h4>
              </div>
              <div class="table-responsive">
                <table class="table">
                  <tbody>
                    <tr>
                      
                    </tr>
                    <tr>
                      <td class="cart_total_label">Shipping</td>
                      <td class="cart_total_amount">
                        <i class="ti-gift mr-5"></i> Free Shipping
                      </td>
                       
                    </tr>
                    <tr>
                      <td class="cart_total_label">Total</td>
                      <td class="cart_total_amount">
                        <strong><span class="font-xl fw-900 text-brand">₹{{subtotal}}</span></strong>
                      </td>
                    </tr>
                  
                  </tbody>
                </table>
              </div>
              <a href="/checkout" class="btn mt-3">
                <i class="fi-rs-box-alt mr-10"></i> Proceed To Checkout
              </a>
            </div>
          </div>
        </div>

      </div>
    </div>
  </section>
</main>
